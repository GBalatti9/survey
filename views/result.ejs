<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trivia bostera</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>

<body>
    <div class="container d-flex flex-column align-items-center justify-content-center" style="min-height: 100vh;">
        <div class="circle border border-primary" style="width: 70px; height: 70px; border-radius: 50%; display: flex; justify-content: center; align-items: center;">
            <p class="m-auto text-primary"><%= result %></p>
        </div>
        <% if(result === '100%') {%>
            <p>Bostero viejo nomás</p>
        <% } else if(result > '50%') {%>
            <p>No está mal</p>
        <% } else { %>
            <p>¿No serás gallina, no?</p>
        <% } %>
        <% if(wrongAnswers.length > 0) {%>
            <!-- && wrongUserChoices.length > 0 -->
            <h4>Respuestas incorrectas</h4>
            <% wrongAnswers.forEach(answer => { %>
                <p class="text-danger"><%= answer.question %></p>
                <div class="container d-flex justify-content-center" style="max-width: 600px;">
                <p class="text-danger" style="box-sizing: border-box;">Tu respuesta: <%= answer.userChoice %></p>
                <% answer.correctAnswer.forEach(el => { %>
                    <p class="text-success" style="box-sizing: border-box;">Respuesta correcta: <%= el.option %></p>
                <% }) %>
                </div>
                <% }) %>
                <!-- <%# wrongUserChoices.forEach(choice => { %>
                    <p class="text-danger"><%#= choice %></p>
            <%# }) %> -->
        <% } %>
        <a href="/">
        <input type="submit" class="btn btn-primary" value="Volver a inicio">
        </a>
    </div>
</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
    crossorigin="anonymous"></script>

</html>